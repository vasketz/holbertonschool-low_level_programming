#include "holberton.h"
#include <stdio.h>

/**
 * mul - check the code for Holberton School students.
 *@a: is a variable
 *@b: is a variable
 * Return: Always 0.
 */
int mul(int a, int b)
{
	int mul = a * b;

	return (mul);
}
