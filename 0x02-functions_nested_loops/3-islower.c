#include "holberton.h"
#include <ctype.h>
/**
 * _islower - check if is lower case
 *
 * Return: Always 0.
 */
int _islower(void)
{

	if (c == islower(c))
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}
