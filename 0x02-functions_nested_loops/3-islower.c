#include "holberton.h"
#include <ctype.h>
/**
 * main - check the code for Holberton School students.
 *
 * Return: Always 0.
 */
int _islower(int c)
{

	if (c == islower(c))
	{
		return(1);
	}
	else
	{
		return(0);
	}
	return (0);
}
